PFont foreignFont;

void setup() {
  // Ensure this font is installed on your computer
  // Otherwise use another unicode font
  foreignFont = createFont("Lantinghei SC", 20); 
  textFont(foreignFont);
  textAlign(CENTER, TOP);
  
  
}

void draw() {
  
}